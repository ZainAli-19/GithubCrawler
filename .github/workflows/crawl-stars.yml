name: Crawl Stars

on:
  workflow_dispatch:

jobs:
  crawl:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install -r src/requirements.txt

      # 🕐 Ensure PostgreSQL is ready before running SQL
      - name: Wait for Postgres service to be ready
        env:
          PGPASSWORD: postgres
        run: |
          echo "⏳ Waiting for Postgres service to be ready..."
          for i in {1..30}; do
            if pg_isready -h postgres -p 5432 -U postgres; then
              echo "✅ Postgres is ready!"
              break
            else
              echo "Postgres not ready yet... waiting 5s"
              sleep 5
            fi
          done
          # Fallback: start local Postgres if container failed
          if ! pg_isready -h postgres -p 5432 -U postgres; then
            echo "⚠️ Container did not start, starting system Postgres as fallback..."
            sudo service postgresql start
            until pg_isready -h localhost -p 5432 -U postgres; do sleep 3; done
          fi

      - name: Setup database schema
        env:
          PGPASSWORD: postgres
        run: |
          echo "📦 Creating tables..."
          psql -h postgres -U postgres -d postgres -f src/db/migrations/001_create_tables.sql || \
          psql -h localhost -U postgres -d postgres -f src/db/migrations/001_create_tables.sql
          echo "✅ Tables created successfully."

      - name: Run crawler
        env:
          POSTGRES_HOST: postgres
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: postgres
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          echo "🚀 Starting GitHub crawler..."
          python src/app.py
          echo "✅ Crawler finished."

      - name: Export database to CSV
        env:
          PGPASSWORD: postgres
        run: |
          echo "📤 Exporting database to CSV..."
          psql -h postgres -U postgres -d postgres -c "\copy (SELECT * FROM repositories) TO 'repos.csv' CSV HEADER" || \
          psql -h localhost -U postgres -d postgres -c "\copy (SELECT * FROM repositories) TO 'repos.csv' CSV HEADER"
          echo "✅ Export complete."

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: github-repos
          path: repos.csv
